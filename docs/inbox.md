# Inbox

> Status: Draft (wizard-generated)
>
> Thin capture layer for unresolved ideas, observations, and items that surfaced during
> sessions but aren't ready for a spec. Triage during reconciliation or session end:
> (a) promote to a spec, (b) promote to an ADR, (c) drop.
>
> This is NOT a task list. Items here are parked thoughts, not committed work.

<!-- Add items below. Format: - [date] description -->

- [2026-08-31] **Real customer prod martech stack captured as the breadth-validation benchmark** → [R-007](research/R-007-real-prod-stack-breadth.md). ~21 tools classified by airlock-fit: the majority fit the two proven archetypes (wire-protocol/pixel + wrapped-SDK); forms (Marketo Forms2 — the formjacking story) + a OneTrust consent driver are new patterns; session-replay/heatmap/chat/identity (FullStory, MS Clarity, LivePerson, LiveRamp) are excluded **by mechanism**; and **RUM/observability (`helix-rum-js`, mPulse) is a strategic host-or-SUBSUME opportunity** — airlock is CWV-first and already measures the signals, and on EDS `sampleRUM` is already on the page. Feeds the MVP5 breadth Split + the post-MVP5 connector roadmap. Parked decisions: a generic **pixel** connector archetype (the big leverage win, ~10 vendors), ~~**airlock-as-RUM-layer** (host vs subsume, EDS coexistence)~~ — **RESOLVED 2026-09-03 (spec 030): replace (core checkpoints)** — airlock replaces the inline `sampleRUM` for `top`/`error`/`cwv`, feed/coexist remain; the interaction/lifecycle set + the creds-gated live wire-shape check stay deferred ([decision](decisions/lightweight-decisions.md) · [connector README](../connectors/helix-rum/README.md)), **Segment** host-vs-replace, **governed form capture**, a **OneTrust** consent driver.
- [2026-09-02] **`build.mjs` needs a third bundle entry point before a real EDS page can ship the pixel connector-selection seam** (spec 026-01). `core/airlock.js`'s `createAirlock({ connector: "pixel", … })` resolves `new URL("./pixel-chamber.worker.js", import.meta.url)` at runtime, but `build.mjs` only bundles `core/chamber.worker.js` + `adapters/eds/index.js` today — a real page selecting the pixel chamber would 404 fetching the worker file. The 026-01 vertical proof is complete at the Node/vitest (FakeWorker-driven) level; this is the remaining step before a live rollout. Small, mechanical — probably folds into 026-02 rather than its own spec. **UPDATE 2026-09-02 (026-02 implementation):** did NOT fold into 026-02 — that slice's scope was locked to config fixtures + adapter wiring + tests (ZERO connector/core/build changes), and neither the spec nor the task explicitly named `build.mjs`. ~~Still open~~ **RESOLVED 2026-09-02 (spec 026-05):** `build.mjs` now emits `core/pixel-chamber.worker.js` as a bundle entry (a same-origin sibling) + its single-worker sibling-layout assertion is generalized to **N workers** (every `new Worker(new URL(...))` in the emitted `eds.js` must reference a known, emitted sibling; no blob:/data: in any chunk — the 004-01 CSP envelope). `npm run build` emits all three siblings (eds/chamber/pixel-chamber); the guard is proven non-vacuous (a dropped worker entry throws). All three pixel vendors (Meta/LinkedIn/Bing) share the one pixel-chamber entry, so it was the single fix. ~~**STILL DEFERRED:** the **`core/dom-chamber.worker.js`** (spec 025-02) bundle entry — grounded exclusion: it is never `new Worker`'d in production (only via its testable host + FakeWorker), so nothing in `eds.js` references it; add it when a real worker-dom tag adapter wires `new Worker("./dom-chamber.worker.js")` (a 025-03+ concern).~~ **RESOLVED 2026-09-02 (spec 025-03 AC6):** `core/airlock.js`'s connector-selection seam grew a third `connector:"dom"` branch (constructs `./dom-chamber.worker.js`, generalizes the init message to `{type:"init", ...connectorConfig}`, GA4/pixel byte-unchanged — regression-pinned in `test/dom-connector-seam.test.js`); the REAL Prism measurement rig (`rig/airlock-mirror-prism-harness.html`) constructs the dom-chamber via this exact seam (`createAirlock({connector:"dom",...})`), making it production-reachable. `build.mjs`'s `WORKER_ENTRIES` now includes `core/dom-chamber.worker.js`; `npm run build` emits all four siblings (eds/chamber/pixel-chamber/dom-chamber) and the N-worker sibling-layout assertion covers it.
- [2026-09-02] **Bing UET's custom-event-goal param abbreviations (`gv`/`ec`) are illustrative, not independently wire-verified** (spec 026-02 AC2, `connectors/pixel/vendors/bing.js`). The endpoint (`bat.bing.com/action/0`) and the base tag's `ti`/`evt=pageLoad` are solidly grounded (R-007:37 + widely-documented); `evt=custom` for a custom "Event" goal and the `gv` (goal value) / `ec` (event category) query-key abbreviations are moderate-confidence, not a captured/cited live beacon (per the ADR-0020 grounding discipline this spec's header names). Doesn't affect the architectural proof (config-only, zero connector code) — flagged so a future slice can tighten wire-fidelity with a real captured `bat.js` network request if higher precision is ever needed.
- [2026-09-02] ~~**`core/pixel-chamber.worker.js` has no egress-confinement first-import guard**~~ — **RESOLVED 2026-09-02 (spec 026-01 craft-review).** Built `core/confine-pixel-chamber.js` (`applyEgressConfinement(self, { withholdFetch: true })`) as the pixel chamber's first import, mirroring `core/confine-ga4-chamber.js`; source-order + withholdFetch regression-pinned in `test/egress-confinement.test.js`. (Follow-up for 026-02: consider folding both `confine-*-chamber.js` modules into one shared `confine-chamber.js`.)
- [2026-09-03] **The unload-critical dispatcher is now connector-generic for the MAPPER but still POST-only — the pixel GET is a follow-on** (spec 030-01). `core/egress.js`'s `createCriticalDispatcher` gained a `mapper` DI (030-01) so RUM's `mapToRum` egresses at page-hide; the slice's ACs also named a `method` option (default POST) to un-defer the **pixel GET** unload dispatch (a pixel at teardown is still DROPPED — `core/airlock.js` `connector==="pixel"` gate — because a GET beacon has no POST body and the dispatcher hardcodes `method:"POST"`, `egress.js:80`). Deferred to a follow-on: add the `method` option + the GET url-shape (params in the URL, no body) so a pixel's unload-critical event dispatches as a GET, and flip the pixel unload-drop gate. Small, bounded; do it when the pixel connector next needs teardown fidelity. RUM (POST) is unaffected + complete.
- [2026-09-03] **`rig/lh-eds.mjs` leaks an npm-build banner into stdout ahead of its JSON — now TWO consumers work around it** (spec 029-02). `lh-eds.mjs` shells `npm run build` with `stdio:"inherit"` before printing its JSON scoreboard, so `rig/cwv-budget.mjs` (007-03) and now `rig/cwv-scoreboard.mjs` (029-02) both parse it via an `extractTrailingJSON` (find the last top-level `{`). The clean **source-side fix** — route lh-eds's build output to **stderr** (or run the build in a separate step) so stdout is pure JSON — would let both consumers drop the work-around and would harden against a future banner-shape change breaking the trailing-JSON heuristic. Small, low-risk; do it when `rig/lh-eds.mjs` is next touched. Until then both consumers are robust to the banner (the heuristic is proven in CI).
- [2026-09-03] **Inspector collector's buffer isolation is SHALLOW — relies on the flat-record invariant** (spec 028-01, `core/inspector/collector.js`). The collector shallow-copies each 009-02 record on write + on query, which fully isolates the buffer **because every enforcement record is currently FLAT** (all values are string|number — enumerated across the three hosts in the 028-01 craft review). If a future emit site ever adds a **nested object/array** value to a record, that reference would be shared across the buffer and every query result, and the AC2 copy test (which mutates only top-level keys) would not catch it. Documented as a deliberate invariant in `collector.js`; the trigger for a deep-copy/freeze-on-write is "a nested-value record is introduced." Not a current bug (no such record exists) — flagged so a future nested-record emit site upgrades the copy.
- [2026-09-03] **No production boot adapter forwards `onDiagnostic` — the spec-028 inspector is blind to prod-booted instances** (spec 030-02 impl-review reconciliation). `bootHelixRum`, `bootMetaPixel`, `bootLinkedInInsight`, and `bootEdsAnalytics` (all `adapters/eds/index.js`) construct `createAirlock({...})` **without** an `opts.onDiagnostic` pass-through, so a real page that boots any of them cannot surface held/stripped/chamber-error records to the 028 inspector collector — the inspector only sees instances constructed with an explicit `onDiagnostic` (tests, rigs). Cross-cutting across ALL four adapters (not RUM-specific), so not a 030-02 fix. Suggested follow-on: thread an optional `opts.onDiagnostic` (or a shared page-level diagnostic sink) through every `boot*` adapter so the inspector can observe production egress governance. Small, uniform, and unlocks the inspector's stated purpose on real pages. Do it when the inspector next needs a production signal, or as a small cross-adapter slice.
- [2026-09-05] **Coarse-consent split — RESOLVED end-to-end (spec 034-01); one orthogonal residual (OQ13-1) parked.** The concern that the in-chamber `setConsent` delegate would defeat the analytics-only flow on the LIVE alloy path was VALIDATED (grounded against `@adobe/alloy@2.35.0`: `collect:"n"` makes `awaitConsent()` reject the send at `createEventManager.js:70`/`:99`, suppressing the interact upstream of the seam) and FIXED as an in-scope part of 034-01 — NOT deferred. The fix is two reconciled halves: **(A)** `connectors/alloy/consent.js`'s `shapeAlloyConsent` now drives `collect:"y"` iff **`analytics_storage`** is granted (was: iff BOTH purposes granted), so alloy stays LIVE when analytics is consented; **(B)** the TRUSTED seam (`core/wrapped-sdk-host.js`) strips the per-event `events[].query.personalization` when pzn is un-granted. Delegate = liveness, seam = trusted enforcement. Proven by the four-combo e2e that drives the REAL delegate and asserts the analytics-only interact FIRED (`test/eds-boot-alloy.test.js`), the delegate unit tests (`test/alloy-consent.test.js`), and the AC6 real-alloy differential (`test/wrapped-sdk-host.test.js`). **Remaining open — OQ13-1 (`demdex`/`ad_storage` cookie-write consent-gating):** relaxing `collect` makes a `demdex`/`ad_storage` cookie WRITE under denied `ad_storage` reachable in the analytics-yes/pzn-no posture (before, `collect:"n"` suppressed all writes). Pre-existing + orthogonal (`ad_storage` gates neither the old nor new `collect`); compensating control = the endpoint ceiling holding the ad-sync EGRESS. Belongs to the OQ13 item-1 family (cookie-write consent-gating, `docs/refinement-todo.md` §OQ13). Live-Edge confirmation of the stripped-interact shape stays a creds-gated residual (013 pattern). Trigger: OQ13 cookie-write work OR a creds-gated live-Alloy re-probe.
- [2026-09-05] **`rig/subtree-install.mjs` carries a THIRD independent copy of the median + TBT/CLS tight-band math** (spec 036-01 grounding). Spec 036-01 extracted `rig/lh-eds.mjs`'s median/armSummary/delta/band engine into the new, importable `rig/lh-core.mjs` (so `rig/lh-live.mjs`'s live/local query-gate harness reuses it rather than re-implementing it — see `rig/lh-core.mjs` + `test/lh-core.test.js`). While grounding that extraction, `rig/subtree-install.mjs`'s own opt-in `WITH_CWV` before/after check (spec 031, AC6) turned up as a THIRD parallel copy of the identical `median` function + the identical `TBT delta <= 50ms AND |CLS delta| <= 0.01` band check (`rig/subtree-install.mjs:207,225-226`) — pre-existing, out of 036-01's declared scope (a different rig, a different spec), not touched here. Suggested follow-on: have `rig/subtree-install.mjs` import `median`/`withinTightBand` (or the fuller `armSummary`/`computeDeltaMedian`) from `rig/lh-core.mjs` instead of its own inline copy, so there is exactly ONE band engine instead of three. Small, mechanical; do it when `rig/subtree-install.mjs` is next touched, or as a tiny standalone consolidation slice.
- [2026-09-05] **Shared rig local-server plumbing is duplicated across `rig/lh-eds.mjs` + `rig/lh-live.mjs`** (spec 036-01 craft review). Both carry the same `BOILERPLATE_CSP` header, `MIME` map, `NOOP_EDS` no-op module, and a near-identical static-serve http skeleton (`lh-eds.mjs:51-85`, `lh-live.mjs:79-178`). 036-01's AC1 *engine* reuse (median/band via `rig/lh-core.mjs`) is satisfied; this is the SERVER plumbing, deliberately not extracted this slice (touching `lh-eds`'s server again = regression risk on a passing rig). Follow-on: factor a shared `rig/` server helper (CSP/MIME/NOOP + static-serve + the query-gate arm derivation) as the rig family grows (036-02 adds more). Small; do it when next touching either rig.
- [2026-09-05] **`rig/lh-live.mjs`'s mode-selection / guard / URL-construction has no CI coverage** (spec 036-01 craft review). Only `rig/lh-core.mjs`'s pure `bandDisposition` is unit-tested, and only the query-gate LOCAL path is exercised by the dry-run; the two-deployment + mutual-exclusion guard branches (`lh-live.mjs:67-77,183-190`) are operator-only. The URL logic is already CORRECT (`new URL()` + `searchParams.set()`, not naive concat — a `LIVE_URL` with an existing query string is handled), so this is regression-protection, not a bug. Follow-on: extract a pure `resolveRunPlan({liveUrl,baselineUrl,adoptedUrl,queryParam})` → `{mode,offUrl,onUrl,error?}` into `rig/lh-core.mjs` + unit-test the guards/mode/URL construction. Do it alongside the server-plumbing consolidation above (both touch the rig family).
- [2026-09-05] **Three small 037 (1.0 API pin) follow-ons.** (a) **Stale seal-staging comment outside the frozen surface** — `connectors/alloy/connector.js:148-149` carries the same "the enforcement teeth are MVP3; the seal is unbuilt" staleness the six frozen contract files had (the seal shipped + enforces via 017-03/020-02/016-02); left untouched by 037-01 because it is a connector IMPL comment, not one of ADR-0017's frozen contract surfaces — reword it present-tense when `connectors/alloy/connector.js` is next touched. (b) **Dual-path `compositeEmit` integration coverage** — 037-01 removed `composite.accepts` + rebound the alloy exposure reporter's `compositeEmit.accepts` to a local `booted` predicate (byte-equivalent, so low-risk); but the real `boot()` rebind is never driven end-to-end by a LIVE alloy+GA4 composite (each piece is covered separately; AC7 uses a hand-rolled `compositeEmit` stub). A single dual-worker alloy+GA4 integration test would close the residual composition gap; disproportionate for 037's surface-narrowing scope, worth it if that infra is built for another reason. (c) **AC2 grep-family/OQ9 tidiness** — spec 037-01's AC2 gate-2 STRIP-class grep family lists `OQ9`, but OQ9's multi-chamber-coherence axis is a legitimate NOT-FROZEN carve-out survivor, so a literal grep false-positives on correct prose; the rewords themselves are correct (the reviewer + the read-through handled it), this is only the AC's backstop-wording — scope the grep family to exclude the coherence carve-out if that gate is ever re-run mechanically. The **manifest `purposes`/`endpoints` mirror-drift** (enforcement reads the hand-wired `egressPurposes`/`endpointCeiling`, not the manifest field) is the larger tracked residual — already recorded in the OQ13/037 refinement-todo context + disclosed in `contracts/connector.d.ts`'s `purposes` docstring; closing it (make the manifest field gate mechanically) is an additive tightening within the frozen shape (no major break).
- [2026-09-07] **Container translator — the post-1.0 (1.1) adoption simplifier** ([ADR-0018](decisions/adr-0018-reframe-onto-adoptable-one-point-oh.md) Emergent E9; vision § Scope). The 1.0 rewire (MVP9) is a *manual, two-party, documented* procedure. The longer-horizon simplifier: take a **Tealium / GTM / Adobe Launch container as input and auto-translate it to an airlock config** (self-hosting the vendor files + auto-optimizing + rewrapping the loading), so adoption is "a breeze". Decomposition (owner conversation, 2026-09-05): **(1) config translation** — parse the container's tag/trigger/variable graph → an airlock `boot(config)` (feasible; bounded by connector coverage — a tag with no airlock connector can't translate); **(2) vendor-runtime replacement** — the actual TBT win, via the protocol connectors MVP7–8 build (the TBT-dominant tags are beacon-shaped); **(3) worker-wrapping the container** — the Partytown-style bet (cautious; the hard part). Explicitly **post-1.0** — 1.0 is proven by the manual rewire first; the translator is 1.1. Parked here; promote to a research note when 1.0 is in sight.
- [2026-09-08] **"026 wire-fidelity" — Meta's `meta.js` emits bare `value`/`currency`/`content_name`/`content_category`, but Meta's documented `/tr` wire form namespaces them under `cd[...]`** (spec 038-01, the parity harness's Meta descriptor, `rig/parity/descriptors/meta.js`). Confirmed again while building the oracle (first surfaced by the 038-01 frame-critique r4/r5, `docs/specs/038-parity-harness/reviews/slice-01-frame-critique.md`): `connectors/pixel/vendors/meta.js:74-81`'s `paramMap` projects these four event-data fields under their bare names, not Meta's `cd[value]`/`cd[currency]`/`cd[content_name]`/`cd[content_category]`. The 038-01 harness deliberately does NOT reconcile this via a wire-name translation (that would be a "map" of a spelling Meta may never ingest — a false pass, ADR-0020's false-shim prohibition) — it models all four as an OWNED gap-map entry (green, `expected-dropped`, owner "026 wire-fidelity") instead, so the parity harness's `pass` is honest without blocking on the fix. No spec/slice currently owns fixing `meta.js` itself to emit the namespaced `cd[...]` form — 026-04 (deferred) is scoped to identity/advanced-matching + the POST/`ctx`-body shape, not this wire-fidelity gap. **Real-driver-gated, like 026-04**: a live Meta `/tr` capture is needed first to confirm whether Meta's endpoint (a) requires the `cd[...]` namespace (then `meta.js` needs a fix, closable by re-running `npm run parity:meta` after) or (b) also accepts the bare spelling (then the harness's `wireNameMap` can safely reconcile it as `maps`, not a gap). Do this when a real Meta capture is available (R5 — captures are local-only, so the confirming step cannot happen in this repo) or when 026 is next touched.
- [2026-09-02] **Two prior slices' `git diff -- core/` "zero core changes" canary tests are now permanently stale** (`test/pixel-config-contract.test.js`'s "ENUMERABLE — `git diff -- core/` is empty" and `test/pixel-vendors.test.js`'s twin — both from spec 026-02, asserting the CURRENT working tree has no uncommitted `core/` changes). Spec 025-03 legitimately touches `core/` (`core/airlock.js`, `core/worker-dom/*`) — a normal, expected future-slice event these two tests never anticipated: they check `git diff` against whatever HEAD is at RUN TIME, not a fixed commit range scoped to 026-02's own PR, so ANY later slice that touches `core/` (correctly, per its own spec) makes them fail forever after, not just transiently. Both FAILED during 025-03's implementation (confirmed as an expected byproduct, not a behavioral regression — no other 026-02 assertion broke). Not fixed here (out of 025-03's declared scope — those test files belong to 026-02). Suggested fix for whoever next touches pixel connector tests: either delete both (their information value was already captured at 026-02's own review time) or scope them to a fixed commit range (e.g. `git diff <026-02-base>..<026-02-head> -- core/`) instead of an open-ended "current working tree" check.
