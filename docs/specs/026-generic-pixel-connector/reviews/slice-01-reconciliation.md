---
slice: 026-01 — Meta Pixel through the generic connector, governed + dispatched (the archetype proof)
pass: reconciliation
verdict: pass
reviewer: jig:reviewer (orchestrator)
reviewed_at: 2026-09-02T22:44:28Z
prompt_source: reconciliation sweep
---

Reconciliation (026-01) — PASS. The slice answered its reframed contract: Meta's /tr GET ships end-to-end through
a genuinely declarative interpreter, governed by the existing seal (consent held/flushed-as-GET/dropped,
endpoint-confined, PII-stripped, no identity leak), via the two bounded core generalizations (connector-selection
seam + three-site method-aware dispatch) resolving OQ10 for GET. GA4's own verdicts, POST dispatch, and unload path
are proven unchanged (co-located regressions + the full GA4/adapter suite, unmodified + green: 840 tests / 72 files).

Gate history recorded, all durable: frame-critique PASS (3 rounds — killed the "zero core changes" premise,
reframed to ship-a-real-beacon, two surgical re-fixes); compliance PASS; craft NEEDS-CHANGES → PASS. The craft
round found a real second GA4-mis-map entry (pushCritical) the frame-critique's own enumeration missed — fixed at
the core (guard + tests), the AC10 causality flaw fixed (granted-consent counterfactual), and the pixel-chamber
egress-confinement guard pulled in (security parity for an ad-vendor chamber). All folded into the Deviation log +
Core-change diff note + AC10 honestly; the confinement inbox item flipped to RESOLVED.

Promoted, no orphans: the paramMap `from`-vocabulary generality + the GET-only→POST method-extension point (a
clean, non-breaking forward boundary since fetchInit already honors r.method) → 026-02's grounding, which should
cite this slice; basic _fbp/fbc cookie identity → a chamber-cookie-capability follow-up; advanced matching → 026-03;
build.mjs's missing third bundle entry → parked in inbox (a real-EDS-rollout gap, out of the Node/vitest scope).
No live identifiers (synthetic 000000000000000), no probe code, no runtime dependency added.
