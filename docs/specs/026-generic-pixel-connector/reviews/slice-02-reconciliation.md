---
slice: 026-02 — more vendors as configs: the archetype generalises across real GET pixels
pass: reconciliation
verdict: pass
reviewer: jig:reviewer (orchestrator)
reviewed_at: 2026-09-03T01:17:41Z
prompt_source: reconciliation sweep
---

Reconciliation (026-02) — PASS. The 026-01 declarative-map interpreter GENERALISES across real vendors: LinkedIn
Insight + Bing UET ship as flat GET configs with ZERO connector/core code (both diffs empty; the central generality
bet HELD, no code escape). Elegant proof: LinkedIn (no event-name key) is pure config via the interpreter's existing
null-omit rule. Gate history durable: frame-critique PASS (after the POST-deferral reframe — the speculative GA4-MP
POST proof was cut as contradictory + YAGNI); compliance NEEDS-CHANGES → PASS; craft PASS. AC4 config-contract axes
(7, sharpest: eventMap value = string|null; the null-omit idiom first-class) promoted to 026-03. Deferred/tracked:
POST/ctx-body + the PixelVendorConfig type → 026-03; Bing gv/ec wire-fidelity caveat + the 3-vendor build.mjs
bundle-entry (shared with the dom-chamber worker) → docs/inbox.md. Durable zero-code guard is the strengthened
no-vendor-string grep (the empty-git-diff test is a dev belt only, documented). No orphans; no live identifiers; no
new dependency.
