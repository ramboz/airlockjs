---
slice: 045-02 — apply hold-until-granted to alloy (via alloy's native `defaultConsent:"pending"` queue, preserving 034-01)
pass: craft
verdict: pass
reviewer: general-purpose (pr-review skill, opus)
reviewed_at: 2026-09-13T01:15:49Z
prompt_source: review.py pr-review --richer-skill pr-review ... 045-02 <deliverables>
substrate: shown
applied_skill: pr-review
shown_candidates: [pr-review:high-confidence, scout-pr-review:high-confidence, servo:agent-loop:high-confidence, servo:autonomy-readiness:high-confidence, servo:quality-gate:high-confidence, access:speculative, adobe-security-antipatterns:speculative, adobe-security-audit:speculative, adobe-security-client:speculative, adobe-security-cloud:speculative, adobe-security-foundations:speculative, adobe-security-lang:speculative, adobe-security-services:speculative, agent-development:speculative, arch-review:speculative, audit-migrator:speculative, block-kit:speculative, build-mcp-app:speculative, build-mcp-server:speculative, build-mcpb:speculative, cardputer-buddy:speculative, claude-automation-recommender:speculative, claude-md-improver:speculative, claude-security:speculative, command-development:speculative, configure:speculative, content-fidelity:speculative, create-slack-app:speculative, cutline:speculative, debug-workflow:speculative, design-eval:speculative, eval-authoring:speculative, example-command:speculative, example-skill:speculative, frontend-design:speculative, get-content-scrape:speculative, hook-development:speculative, investigate-alert:speculative, local-dev:speculative, m5-onboard:speculative, math-olympiad:speculative, mcp-integration:speculative, morning-ai-radar:speculative, morning-assistant:speculative, morning-confluence:speculative, morning-github:speculative, morning-jira:speculative, morning-outlook:speculative, morning-slack:speculative, morning-spike:speculative, mysticat-debug:speculative, playground:speculative, plugin-settings:speculative, plugin-structure:speculative, project-artifact:speculative, query-audits:speculative, query-opportunities:speculative, query-scrapes:speculative, query-sites:speculative, receipts:speculative, release-check:speculative, release-slate:speculative, run-preflight:speculative, scope-audit:speculative, scout-autotune:speculative, scout-bench-create:speculative, scout-memory-init:speculative, scout-scrum-master:speculative, servo:edd-suitability:speculative, servo:execution-planner:speculative, servo:heartbeat:speculative, servo:oracle-hook:speculative, servo:scaffold-init:speculative, servo:spec-oracle:speculative, session-report:speculative, shape-release:speculative, silence-alert:speculative, skill-creator:speculative, skill-development:speculative, slack-api:speculative, slack-cli:speculative, slack-docs:speculative, slack-messaging:speculative, slack-search:speculative, spacecat-configuration:speculative, steward:speculative, test-pr-in-dev:speculative, webpage-replica:speculative, writing-hookify-rules:speculative]
---

VERDICT: pass

REASONING:
The craft is high. `shapeAlloyBoot` is a genuinely pure mapping that reuses the unchanged `shapeAlloyConsent` for the y/n paths (no duplication), the host→chamber→alloy wiring traces correctly end-to-end, and the fire-and-forget flush records a rejected `setConsent` in `summary.consentDriven` rather than swallowing it. The wiring is meaningfully guarded across three surfaces (the `shapeAlloyBoot` mapping, the connector `defaultConsent` threading, and the host/adapter message posting + seam hold), each non-vacuous, with the vendor queue/flush mechanism grounded by a creds-free rig. No blockers; the only findings are low-severity edges suitable for the deviation log.

SPECIFIC ISSUES:
- [nit][spec] connectors/alloy/alloy-chamber.worker.js:535 — `applyConsent` routes every mid-session update through `shapeAlloyConsent` (y/n) unconditionally. A mid-session `setConsent` that leaves `analytics_storage` still unresolved drives `collect:"n"`, which per consent.js:22-26 makes alloy's `awaitConsent()` reject — turning the pending "hold" into a "drop" for events queued before that update. The primary pending→grant flow is correct, and pending→denied dropping is arguably correct; only a pending→(partial, still-pending) transition loses the pre-update queue. The spec framed AC3 as the grant flush and named double-fire (A3) but did not enumerate non-grant mid-session transitions. Untested edge; consider skipping the drive (or keeping the hold) when the shaped result is `collect:"n"` while a pending hold is active, or name it as an assumption.
- [nit][impl] connectors/alloy/alloy-chamber.worker.js:485,495,540,541 — `summary.consentDriven` is a single last-write-wins field shared by the boot path and `applyConsent`, so repeated mid-session updates overwrite prior outcomes. Harmless in the primary flow (boot leaves it `null` under pending, so the flush result survives), but a minor observability limitation if setConsent is driven more than once.
- [strength][impl] connectors/alloy/consent.js:146-159,138 — `shapeAlloyBoot` is a clean pure function that delegates all y/n shaping to the unchanged `shapeAlloyConsent`; the purity claim is real. No logic duplicated.
- [strength][impl] connectors/alloy/alloy-chamber.worker.js:486-495,540-541 — a rejected alloy `setConsent` is recorded (`summary.consentDriven = "rejected: …"`) in both the boot path and the fire-and-forget `applyConsent`, so a failed flush is observable via `getState()`, not silently lost.
- [strength][impl] adapters/eds/index.js:1379-1382 — the grant does both halves in the correct order (mutate the seam's live `consentRef` first, then trigger alloy's native flush) and never re-drives past events, honoring the grant end-to-end while avoiding the A3 double-fire. Back-compat airtight: gated on `consentRef && v`.
- [strength][impl] rig/alloy-consent-pending.mjs:99-113 — the grounding rig is creds-free, self-contained, synthetic ids, exits non-zero on failure with explicit pass criteria. Genuinely grounds the vendor queue/flush mechanism.

RECONCILIATION NOTES:
- Coverage split is honest and reasonable, not a blocker. The chamber glue is not directly executed by any test (worker not importable); each half is unit-tested (`shapeAlloyBoot` mapping; `defaultConsent`→configure threading; message posting + seam hold) and the vendor mechanism is rig-grounded — matching how the repo already tests worker code (seam tests + rigs). One-line deviation-log entry.
- Vacuous-test check: the pending→queue guards are non-vacuous (revert to `collect:"n"` fails the `shapeAlloyBoot` PENDING tests + the connector `defaultConsent` test + the integration hold/egress assertions).
- Log the nit-1 mid-session non-grant edge and the ADR-0023 A1 amendment (already in the slice close-out) in the deviation log.

---
Reviewer substrate: general-purpose subagent (Opus), read-only, richer pr-review rubric. Verified shapeAlloyBoot purity, the host→chamber→alloy wiring end-to-end, rejected-setConsent recording, back-compat gating, and non-vacuous tests across three surfaces.
