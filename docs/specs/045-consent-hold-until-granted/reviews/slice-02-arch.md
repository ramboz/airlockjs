---
slice: 045-02 — apply hold-until-granted to alloy (via alloy's native `defaultConsent:"pending"` queue, preserving 034-01)
pass: arch
verdict: pass
reviewer: general-purpose (arch-review skill, opus)
reviewed_at: 2026-09-13T01:15:50Z
prompt_source: review.py arch-review --richer-skill arch-review ... 045-02 <deliverables>
substrate: shown
applied_skill: arch-review
shown_candidates: [arch-review:high-confidence, access:speculative, adobe-security-antipatterns:speculative, adobe-security-audit:speculative, adobe-security-client:speculative, adobe-security-cloud:speculative, adobe-security-foundations:speculative, adobe-security-lang:speculative, adobe-security-services:speculative, agent-development:speculative, audit-migrator:speculative, block-kit:speculative, build-mcp-app:speculative, build-mcp-server:speculative, build-mcpb:speculative, cardputer-buddy:speculative, claude-automation-recommender:speculative, claude-md-improver:speculative, claude-security:speculative, command-development:speculative, configure:speculative, content-fidelity:speculative, create-slack-app:speculative, cutline:speculative, debug-workflow:speculative, design-eval:speculative, eval-authoring:speculative, example-command:speculative, example-skill:speculative, frontend-design:speculative, get-content-scrape:speculative, hook-development:speculative, investigate-alert:speculative, local-dev:speculative, m5-onboard:speculative, math-olympiad:speculative, mcp-integration:speculative, morning-ai-radar:speculative, morning-assistant:speculative, morning-confluence:speculative, morning-github:speculative, morning-jira:speculative, morning-outlook:speculative, morning-slack:speculative, morning-spike:speculative, mysticat-debug:speculative, playground:speculative, plugin-settings:speculative, plugin-structure:speculative, pr-review:speculative, project-artifact:speculative, query-audits:speculative, query-opportunities:speculative, query-scrapes:speculative, query-sites:speculative, receipts:speculative, release-check:speculative, release-slate:speculative, run-preflight:speculative, scope-audit:speculative, scout-autotune:speculative, scout-bench-create:speculative, scout-memory-init:speculative, scout-pr-review:speculative, scout-scrum-master:speculative, servo:agent-loop:speculative, servo:autonomy-readiness:speculative, servo:edd-suitability:speculative, servo:execution-planner:speculative, servo:heartbeat:speculative, servo:oracle-hook:speculative, servo:quality-gate:speculative, servo:scaffold-init:speculative, servo:spec-oracle:speculative, session-report:speculative, shape-release:speculative, silence-alert:speculative, skill-creator:speculative, skill-development:speculative, slack-api:speculative, slack-cli:speculative, slack-docs:speculative, slack-messaging:speculative, slack-search:speculative, spacecat-configuration:speculative, steward:speculative, test-pr-in-dev:speculative, webpage-replica:speculative, writing-hookify-rules:speculative]
---

VERDICT: pass

REASONING:
The change is architecturally sound. It adds hold-until-granted to alloy by driving alloy's own native `defaultConsent:"pending"` queue, wired through one additive host→chamber `setConsent` message; the trusted seam in `core/wrapped-sdk-host.js` is byte-unchanged and stays the independent enforcement backstop, the core seal (`core/consent.js`/`core/airlock.js`) is untouched, and 034-01's analytics-flows/pzn-strip is preserved. The layering is correct: vendor mechanism (native queue) as the untrusted-chamber liveness delegate, strict `egressVerdict` at the seam as the trusted enforcement. One bounded liveness edge exists on the mid-session path (a still-pending update discards the held queue instead of continuing to hold), but it is privacy-safe, outside the ACs, and low-likelihood, so it logs rather than blocks.

SPECIFIC ISSUES:
- [strength][impl] core/wrapped-sdk-host.js:602 — the new `setConsent` is a thin additive `postMessage({type:"setConsent"})`. The seam enforcement (`dispatchInterceptedFetch` strict verdict at :360, the 034-01 per-event personalization strip at :388-402, the held-at-seal drop at :361-379) is byte-unchanged. `git diff` shows only two hunks: the JSDoc return-type line (~:164) and this method (~:588-607).
- [strength][impl] core/wrapped-sdk-host.js:355,360 + adapters/eds/index.js:1381-1382 — the seam reads the LIVE `consent` closure ref per-interact, and the adapter mutates that same object (`Object.assign(consentRef, v)`) BEFORE triggering the chamber flush. So on grant, both the set-consent request and each flushed interact are gated by the strict verdict under the now-granted consent. A compromised chamber firing under pending is still strict-dropped at the seal (:361-379). The 020-02 "seam does not trust the chamber" invariant holds.
- [strength][impl] connectors/alloy/consent.js:146-159 — `shapeAlloyBoot` is a pure, unit-testable shaper; native `defaultConsent:"pending"` threads through `configureExtras` (connector.js:102) into alloy's `configure` (connector.js:198), grounded against the real 2.35.0 bundle by rig/alloy-consent-pending.mjs. Lean; no speculative knobs; the two rejected alternatives are documented with grounding. The null-vector vs wired-pending distinction is sound (mirrors GA4).
- [nit][impl] connectors/alloy/alloy-chamber.worker.js:530-541 — the mid-session `applyConsent` uses `shapeAlloyConsent` (y/n only, :535), not the pending-aware `shapeAlloyBoot`. `shapeAlloyConsent` maps a non-null still-pending vector to `collect:"n"` (fail-closed). Driving `setConsent(collect:"n")` while alloy holds under `defaultConsent:"pending"` discards the queued events (consent.js:22-26), rather than continuing to hold — asymmetric with the boot path. It only bites when a mid-session partial merge leaves `analytics_storage` pending; grant→flush and deny→drop are both correct. Privacy-safe (fails toward no-egress), outside AC3/AC4, low-likelihood if adopters resolve consent atomically.

RECONCILIATION NOTES:
- Log the three strengths (seam byte-unchanged git-diff-verified; live-consent-ref aliasing keeps the flushed interact seam-gated; native-queue layering with rig grounding).
- Log/FIX the `applyConsent` asymmetry (worker.js:530-535): consider routing `applyConsent` through `shapeAlloyBoot`-style logic so a still-pending merged vector skips `setConsent` and keeps holding, matching the boot path. Not a blocker.
- ADR-0023 A1 amendment: correctly deferred to the slice close-out; confirm it cites rig/alloy-consent-pending.mjs as grounding.
- Minor, no action: consent state lives in three places (host closure `consent` = adapter `consentRef`; the chamber's retained `consentVector`). Safe by design — the seam copy is the trusted enforcement, the chamber copy is untrusted liveness; divergence cannot cause over-egress.

---
Reviewer substrate: general-purpose subagent (Opus), read-only, richer arch-review rubric. Verified the seam byte-unchanged (git diff), the live-consent-ref aliasing, the native-queue layering + rig grounding, core-seal untouched, and 034-01 preserved.
