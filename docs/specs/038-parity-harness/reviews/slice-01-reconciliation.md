---
slice: 038-01 — vendor-generic harness core + same-protocol oracle (Meta Pixel)
pass: reconciliation
verdict: pass
reviewer: jig:reviewer
reviewed_at: 2026-09-08T17:06:43Z
prompt_source: review.py reconciliation docs/specs/038-parity-harness/spec.md 'Meta Pixel'
---

VERDICT: **pass** (round 2, independent `jig:reviewer`, read-only). Round 1 returned needs-changes on ONE defect — the reconciliation sweep falsely claimed `docs/specs/README.md` was "regenerated → DONE" while the board showed `DRAFT` and the slice is `REVIEWED`. Corrected: the disposition is now `deferred (close-out)` — the board is regenerated by `workflow.py status-board` only after the `→ DONE` transition, so it correctly still shows pre-DONE. Round 2 independently verified the fix against the board row + slice frontmatter and passed.

The deviation log is substantively faithful — all claims verified against source and the three recorded pass reviews: the `ParityDescriptor` typedef now carries `endpoint` + `deriveLogicalEvent`; `redact.js` scrubs `fbclid`/click-ids from `dl`/`dr` with a regression test (a real R5 hole — a live `fbclid` in `dl` is the id `fbc` encodes); the vacuous `emitted` assertion uses the real replay; the `cd[...]` gap is modelled as owned `gapMap` entries (never a false `wireNameMap`), the 026 fix parked in `docs/inbox.md`; the deferred items (untested `wireNameMap` identity-fallback branch; `matchesPattern`→`lh-core` extraction — the 2nd copy, out of scope; the completeness-guard owned by ADR-0020) are honest and non-blocking.

Round-2 non-blocking sweep-completeness nits — all folded: added `package.json` (`parity:meta`) + a spec-authoring row (spec.md / slice-02 / slice-03 = the 5-round reframe + gap-map alignment) to the sweep, and clarified `docs/decisions/README.md` to `no-op (for 038-01)`. A cumulative-branch scope preamble now states the branch also carries prior MVP7 units (ADR-0019/0020, R-009/010, releases, spec 039, lh-core/lh-r010), each reconciled in its own unit and not 038-01 deliverables.

Scope is clean (the redact.js fold is a legitimate security fix, not creep); tech-debt follow-ups appropriately tracked.
