---
slice: 044-02 — g-ads opts into hold-until-granted (denied-consent parity)
pass: craft
verdict: pass
reviewer: general-purpose (pr-review skill, opus)
reviewed_at: 2026-09-12T17:33:50Z
prompt_source: review.py pr-review --richer-skill pr-review docs/specs/044-google-ads-connector/spec.md 044-02 <deliverables>
substrate: shown
applied_skill: pr-review
shown_candidates: [pr-review:high-confidence, scout-pr-review:high-confidence, servo:agent-loop:high-confidence, servo:autonomy-readiness:high-confidence, servo:quality-gate:high-confidence, access:speculative, adobe-security-antipatterns:speculative, adobe-security-audit:speculative, adobe-security-client:speculative, adobe-security-cloud:speculative, adobe-security-foundations:speculative, adobe-security-lang:speculative, adobe-security-services:speculative, agent-development:speculative, arch-review:speculative, audit-migrator:speculative, block-kit:speculative, build-mcp-app:speculative, build-mcp-server:speculative, build-mcpb:speculative, cardputer-buddy:speculative, claude-automation-recommender:speculative, claude-md-improver:speculative, claude-security:speculative, command-development:speculative, configure:speculative, content-fidelity:speculative, create-slack-app:speculative, cutline:speculative, debug-workflow:speculative, design-eval:speculative, eval-authoring:speculative, example-command:speculative, example-skill:speculative, frontend-design:speculative, get-content-scrape:speculative, hook-development:speculative, investigate-alert:speculative, local-dev:speculative, m5-onboard:speculative, math-olympiad:speculative, mcp-integration:speculative, morning-ai-radar:speculative, morning-assistant:speculative, morning-confluence:speculative, morning-github:speculative, morning-jira:speculative, morning-outlook:speculative, morning-slack:speculative, morning-spike:speculative, mysticat-debug:speculative, playground:speculative, plugin-settings:speculative, plugin-structure:speculative, project-artifact:speculative, query-audits:speculative, query-opportunities:speculative, query-scrapes:speculative, query-sites:speculative, receipts:speculative, release-check:speculative, release-slate:speculative, run-preflight:speculative, scope-audit:speculative, scout-autotune:speculative, scout-bench-create:speculative, scout-memory-init:speculative, scout-scrum-master:speculative, servo:edd-suitability:speculative, servo:execution-planner:speculative, servo:heartbeat:speculative, servo:oracle-hook:speculative, servo:scaffold-init:speculative, servo:spec-oracle:speculative, session-report:speculative, shape-release:speculative, silence-alert:speculative, skill-creator:speculative, skill-development:speculative, slack-api:speculative, slack-cli:speculative, slack-docs:speculative, slack-messaging:speculative, slack-search:speculative, spacecat-configuration:speculative, steward:speculative, test-pr-in-dev:speculative, webpage-replica:speculative, writing-hookify-rules:speculative]
---

VERDICT: pass

REASONING:
The change is small, correct, and well-tested. `createGoogleAdsRemap` is genuinely pure (injected `readCookieString`, no `document`/globals, re-read fresh inside the returned closure at flush), its signature lines up with the seal's `remap(event, consent)` call site (core/airlock.js:747), and attaching `event` is scoped to the g-ads `page_view` path so it does not leak into other connectors. The load-bearing re-map test is non-vacuous: it asserts the flushed URL carries granted `gcs`/`npa` plus a fresh `auid` and explicitly not the stale `gcs=G100`/`npa=1`, and the boot-time denied beacon it contrasts against is asserted to hold those stale values, so the test would fail on a stale verbatim re-send. The one gap is coverage-only and non-blocking.

SPECIFIC ISSUES:
- [nit][spec] test/google-ads-seal.test.js:227-230 (and connector.js:196) — the re-map's `gcd` flip is uncovered. connector.js:196 doc claims it "re-encodes `gcs`/`gcd`/`npa`", but AC2/AC4 name only `gcs`/`npa`, so the test asserts only those two. `gcd` is emitted here (`consentDefault` unset → denied-all default → present) and flips on grant, yet no assertion covers it. A regression that cached the boot-time `gcd` would stay green. Root is the AC frame naming `gcs`/`npa` not `gcs`/`gcd`/`npa`, so a clarify pass could have caught it.
- [nit][impl] connector.js:214-219 — `createGoogleAdsRemap`'s `consentDefault` (→ `gcd`) and `landingUrl` (→ inbound click-id re-discovery) params are forwarded but exercised by no test in this slice (all tests omit both). The re-map's click-id/`gcd` branches ship untested.
- [strength][impl] connector.js:214-226 — clean purity. `readCookieString()` is invoked fresh inside the returned closure, never cached, so a linker cookie arriving during the hold is picked up at flush; mirrors the `sourceGa4Ctx` injection seam and stays directly Node-testable.
- [strength][impl] connector.js:176 — the `event` attach is connector-local. Other connectors' `EgressRequest`s keep `event === undefined`, so the seal's `canRemap` gate (core/airlock.js:452) is false for them and their behavior is byte-unchanged. No leak, additive-only.
- [strength][impl] test/google-ads-seal.test.js:172-177,227-233 — the re-map test is a real regression guard, not a line-toucher: it pins the stale denied payload (`gcs=G100`/`npa=1`/no `auid`) on the boot beacon, then asserts the flushed beacon flips all three, so it fails on a stale re-send. This directly satisfies the vacuous-test check.

RECONCILIATION NOTES:
- Log the two nits in the deviation log rather than block REVIEWED. Suggested follow-up: add one assertion on the re-mapped `gcd` denied→granted flip, and (if the click-id-at-flush path is meant to matter) one `landingUrl` re-map case. Consider aligning AC2/AC4 wording to `gcs`/`gcd`/`npa`.
- Informational, not a nit: `handle` attaches the full source `event` to every g-ads beacon, including the granted immediate-send path, so it rides the worker→main clone even when never held. By-design (the connector can't know the consent verdict in the worker) and the payload is plain clone-safe `AirlockEvent` data (§A2), so an accepted cost, not a finding.
- Strengths worth carrying: the purity seam, the connector-local `event` attach, and the non-vacuous re-map test are patterns the sibling ad connectors (Floodlight/Meta opt-ins) should copy verbatim.

---
Reviewer substrate: general-purpose subagent (Opus), read-only, richer pr-review rubric. Verified against the seal contract (core/airlock.js:452,747), sourceGoogleAdsCtx (cookies.js:90), the consent encoders, and EgressRequest.event (contracts/connector.d.ts:105).
