---
adr: 0014
pass: frame-critique
verdict: pass
reviewer: jig:reviewer (independent, re-verified)
reviewed_at: 2026-09-02T16:55:14Z
prompt_source: review.py frame-critique + re-verify
---

Frame-critique — ADR-0014 (Lever-2 minimal worker-dom mirror, Tier 0). VERDICT: pass (after must-fix resolution; independently re-verified). Initial verdict FAIL — not on the direction (minimal-mirror over wrap/Partytown; Tier-0-only; SAB deferred are settled maintainer choices) but on honest-reasoning framing: the value story ("Lever 1 + Lever 2 cover MOST of the space; Tier 0 = a useful slice") did unearned work. THREE must-fix folded + re-verified pass: (1) breadth overclaim de-claimed — new "Coverage — the honest bound" states Tier 0 covers a likely MINORITY of costly tags (the worst/most-common are sync-read = the Tier-0 gap); (2) the "adapt to Lever 1" escape downgraded to PARTIALLY grounded (023-01 proved one chunkable fixture, not QSA-heavy/monolithic; the batched-read capability 023-02 is unbuilt); (3) the main-thread mutation-apply INP-safety flagged as the central UNVALIDATED bet (spike AC3 deferred = the build-spec's first probe). Should-consider folded: R-007 reframed as the wrong yardstick (connector-fit, not DOM-cost); Option A (wrap) given real weight + the unbounded-subset risk; Tier-1 owns that it re-incurs the embed breakage AD-4 avoided. The decision reasons its risks + assumptions honestly — the honest posture a pre-acceptance frame gate exists to secure.
