---
adr: 0018
pass: frame-critique
verdict: pass
reviewer: jig:reviewer
reviewed_at: 2026-09-07T21:18:52Z
prompt_source: review.py frame-critique docs/decisions/adr-0018-reframe-onto-adoptable-one-point-oh.md
---

Frame-critique verdict: **pass** (on ADR-0018 as of the r6 state, commit `39dd801`; r7 then applied this reviewer's three non-blocking reconciliation notes as wording-precision fixes without changing the frame).

This was the 6th adversarial round. Rounds r1–r5 each returned `needs-changes` on genuinely load-bearing frame issues, all addressed:
- r1 → r2: the ADR is a re-read of ADR-0017, not a supersession (correct disposition); GA4 is emitted via the Measurement Protocol, not the container's `gtag.js` `/g/collect` beacon, so "parity" cannot be a same-protocol beacon diff for GA4 → parity redefined as *semantic at the vendor boundary* with per-protocol oracles; GA4 parity made MVP7 risk-first, not a confirmation step; R1 recorded as a partial amendment of ADR-0017's "trust across minor versions" consequence for the 0.x window.
- r3: transport is part of the vendor boundary — the container's Meta/Ads/Floodlight tags carry the vendor's cross-site cookie (`fr`, `IDE`) that airlock's `fetch` egress omits by construction (`core/airlock.js:48-50`, no `credentials`/`mode` anywhere in core/adapters/connectors/contracts) → parity measured on both cookie cohorts, credential context captured+replayed, and the credentialed-transport question deferred to a follow-on ADR (E10) *made visible, never absorbed*.
- r4: the rewire is two-party — the reference site's container is customer-owned (`ies-erp`), so the after-arm and the console leg are the container owner's to produce; named as an assumption with its own kill criterion.
- r5: the console leg has two levels — event-level receipt (lab-observable) vs attribution (live-traffic only); before-arm = the container as shipped.
- r6 (this round): the CWV win is reported-not-repo-grounded → R-010 indicative bound (E11) added as MVP7 risk-first, a CWV threshold + kill criterion added, E12 win semantics; the after-arm made same-host query-gated (avoiding 036's cross-deployment confound); the GA4 `api_secret` browser-exposure adoption blocker added to R-009(a) and the GA4 kill criterion.

**Verdict reasoning (this round):** The frame survives the strongest attack. The single highest-risk load-bearing assumption is **container-owner cooperation** — 1.0's proof depends on the customer-owned Tealium container owner (a party the repo owner does not control and whom no corpus artifact shows as engaged). But the ADR names this as assumed-not-grounded, grounds the ownership split in `intuit-erp/MARTECH.md`, and gives it a dedicated kill criterion (the ladder pauses at v0.9.0; the owner re-decides, with an event-level-only bar allowed only as an explicit re-decision, never a silent narrowing) — so per the reconciliation rules it is a known residual, not a block. Every other named assumption (GA4-via-MP parity incl. the `api_secret` blocker, Ads/Floodlight protocol reproducibility, the transport/cookie divergence, the reported CWV win) is routed to a kill criterion that trips visibly; the dispositions are sound and grounded; the parity / before-after / console-leg definitions are internally consistent against the repo's actual state.

**Grounding the reviewer independently confirmed:** `core/airlock.js:48-50` (no credentials/mode) and `adapters/eds/index.js:1008-1013` (round-trip dispatch equally credential-less, reads the response); `connectors/ga4/map.js:82-84` (`api_secret` in the browser-side URL — the E6 adoption-blocker is real); `rig/lh-core.mjs` (≤50ms *preservation* band, not a win band → E12 gap genuine; `blockedUrlPatterns` unused by any rig → E11 holds); `test/contract-stability.test.js` pins type-signature substrings via `toContain`, never comment text, and `test/instrumentation-config-contract.test.js:236-239` asserts on the *root README* not the schema → the retrofit's "disjoint from the pins" reason and the README "keep NOT frozen" constraint are both grounded; `reserve-personalization.js:51` grounds the 018-h "de-facto closed" reconciliation; `package.json` 0.5.0 + `postversion→release:dist` grounds E1; `MARTECH.md` ownership split / hostname-selected env / opt-in phase-split / querystring-gated native-filter-rule mechanism all match; ADR-0017's "trust across minor versions" consequence is exactly what R1 amends, and `amend`+`reaffirm` (not `supersede`) is correct since the contract content survives.

**Coverage floor:** independently re-probed (the `1.0` literal + premise-phrasing tokens across docs/, contracts/, README, CHANGELOG, code tree); no unaccounted dead-premise carrier found — every out-of-manifest hit is a spec-name reference, `THRESHOLD=1.0`, or a version string. The floor owns its residual with the T1 post-reframe-discovery backstop.

**Non-blocking reconciliation notes (all applied in r7 as wording precision, frame unchanged):**
1. R-010 "upper bound / bounds what MVP9 can ever show" was an over-claim — `blockedUrlPatterns` strips the vendor *runtimes* but not the container's per-template `PINIT`/`INIT` init, which MVP9's native exclusion also removes → reworded to *indicative bound, not a hard ceiling* everywhere it appeared (Context, MVP7 ladder row, E11, Assumptions, CWV kill criterion).
2. The phase-split parenthetical listed two deferred tags; `MARTECH.md` shows three (Floodlight 9, Google Ads 15, Facebook/Meta 21) → Meta added.
3. "TBT-dominant generic vendor tags (GA4, …)" was loose — the cited 601.5→204ms is the three ad runtimes; GA4 is the parity *anchor*, not a TBT contributor → reworded in both the Context and the ruling.
4. (Left for E10's own frame-critique, not ADR-0018:) E10's proposed `mode:"no-cors"` transport yields an opaque response, but the wrapped-SDK round-trip `egress.dispatch` seam reads its response — a design constraint for the E10 follow-on ADR, which is gated on the `ad_storage` purpose class (alloy's round-trip is not `ad_storage`) and carries its own frame-critique.
